# Spec: `log_interaction()`

**File:** `auditor.py`
**Status:** Spec incomplete — fill in all blank fields before implementing

---

## Purpose

Record every interaction — question, safety tier, and response preview — to an audit log file. Audit logs exist so that the system's behavior can be reviewed after the fact: to catch systematic errors, monitor for unexpected patterns, and provide accountability if something goes wrong.

---

## Input / Output Contract

**Inputs:**

| Parameter  | Type  | Description                                          |
| ---------- | ----- | ---------------------------------------------------- |
| `question` | `str` | The user's home repair question                      |
| `tier`     | `str` | The safety tier assigned by `classify_safety_tier()` |
| `response` | `str` | The response generated by `generate_safe_response()` |

**Output:** `None` — side effects only (writes to file, prints to terminal)

---

## Design Decisions

_Complete the fields below before writing any code._

---

### Log entry fields

_The four required fields are already in the table below. Add at least two more that you think a developer reviewing this log would actually need._

_Think about what you'd want to see if you discovered a cluster of 200 logged questions where the classifier was consistently wrong. What's missing from just the four required fields that would help you diagnose it?_

| Field                     | Type   | Description                                                                                                                                                                                                               |
| ------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `"timestamp"`             | `str`  | ISO 8601 datetime (UTC) — `datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")`                                                                                                                                 |
| `"tier"`                  | `str`  | Safety tier assigned to this question                                                                                                                                                                                     |
| `"question"`              | `str`  | The user's question, truncated to 300 characters                                                                                                                                                                          |
| `"response_preview"`      | `str`  | First 200 characters of the generated response                                                                                                                                                                            |
| `"classification_reason"` | `str`  | The concise reason returned by the classifier explaining why the question was assigned to the selected tier. Useful for identifying systematic misclassification patterns.                                                |
| `fallback_used`           | `bool` | Indicates whether the system used a fallback because classification parsing failed, tier validation failed, or an unknown tier was returned. Helps distinguish LLM failures from normal classifications during debugging. |

---

### Why these truncation limits?

_The required fields truncate the question to 300 characters and the response to 200. Write down the reasoning for each — what would you lose by truncating more aggressively, and what's the risk of logging the full text at production scale?_

```
Question (300 characters):
300 characters usually capture enough context to understand the repair request and why it was classified. A shorter limit could remove important details, while storing the full question increases storage use and may retain unnecessary user information.

Response Preview (200 characters):
200 characters are enough to verify whether the response matched the assigned safety tier. Logging the full response would use more storage and retain more generated content than is necessary for auditing.

```

---

### Directory creation

_What happens if `logs/` doesn't exist when the function runs for the first time? How will you handle that — and why is this worth thinking about at all?_

```
If the logs/ directory does not exist, the function should create it automatically before writing the log file (e.g., using os.makedirs("logs", exist_ok=True)).

This is important because the first run of the application—or a fresh deployment—would otherwise fail with a file error. Automatically creating the directory makes the logging system more reliable and ensures audit logs are always recorded without requiring manual setup.

```

---

### Console output

_Write an example of what you want the one-line terminal summary to look like after a question is logged. Be specific about format._

````
```text
[AUDIT] 2026-06-27T14:35:21Z | Tier: caution | Question: "Can I replace my bathroom faucet?" | Logged to logs/audit.jsonl
````

```

---

## Implementation Notes

_Fill this in after implementing._

**The actual log file content after 3 test queries (paste the three JSON lines):**

```

{"timestamp": "2026-06-27T11:03:18.703283Z", "tier": "safe", "question": "How do I unclog a slow bathroom drain?", "response_preview": "To unclog a slow bathroom drain, follow these steps:\n\n1. **Remove any visible blockages**: Check the drain for any visible hair or debris. Use a pair of needle-nose pliers or a drain screen to remove "}
{"timestamp": "2026-06-27T11:04:38.594630Z", "tier": "refuse", "question": "Can I add a new electrical outlet to my garage?", "response_preview": "I can't provide instructions on how to add a new electrical outlet to your garage. This task involves working with electrical wiring, which can be hazardous if not done correctly. Improper installatio"}
{"timestamp": "2026-06-27T11:09:46.046547Z", "tier": "caution", "question": "How do I reset a GFCI outlet that won't reset?", "response_preview": "Resetting a GFCI outlet that won't reset requires care and appropriate precautions to avoid electrical shock or further damage. To reset the outlet, follow these steps:\n\n1. **Ensure your safety**: Bef"}

```

**One field you'd add to the log if this were a real production system handling 10,000 questions per day:**

```

| Field          | Type  | Description                                                                                                                                           |
| -------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `"request_id"` | `str` | A unique identifier (e.g., UUID) for each interaction, making it easy to trace, search, and correlate specific requests across logs and debug issues. |

```

```
